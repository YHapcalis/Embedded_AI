# 任务拆解（Task Breakdown）

> 文件：`02_任务拆解.md`
> 依据：`01_目标定义.md`（范围 + 成功标准 S1-S10）
> 输入：问题清单（P1-P4）+ 环境依赖审计结论
> 原则：每个任务可独立验收，验收项与 S1-S10 一一对应

---

## 〇、问题清单回顾（任务拆解的直接输入）

| # | 问题 | 结论 | 对应任务 |
|---|------|------|---------|
| P1 | SWD 能否覆盖主流 MCU/SoC？ | 只覆盖 ARM 生态，错过 ESP32/RISC-V | T1.2 |
| P2 | 是否预封装 JTAG？ | 必须，双协议且接口不变 | T1.2 |
| P3 | STM32CubeIDE 内置 OpenOCD 能否换芯片继续用？ | 不能，需多分支共存（ST/Espressif/主线） | T0.3 |
| P4 | VSCode 高度定制 STM32 如何应对换芯片？ | 板卡 Profile + 自动生成 VSCode 配置 | T2.2, T2.3 |

**环境依赖审计**：高依赖集中在"工具链定位层"（OpenOCD 路径/gcc/Cortex-Debug/批处理），
业务逻辑层天然低依赖 → 解耦重点 = 环境探测层 + 板卡 Profile。

---

## 一、任务清单总览

```
Phase 0  项目骨架与环境基线     （P3 解决）
Phase 1  抽象层核心引擎         （P1/P2 解决）
Phase 2  harness CLI 统一入口   （P4 解决）
Phase 3  MCP 服务封装           （跨工具接入）
Phase 4  技能包知识库           （AI 诊断知识）
Phase 5  模板与标准流程         （开箱即用）
Phase 6  全量验收               （S1-S10）
```

---

## Phase 0：项目骨架与环境基线

### T0.1 初始化项目结构与版本管理
- **目标**：`E:\嵌入式AI工作流\` 建立规范目录，git 初始化
- **产出**：
  ```
  E:\嵌入式AI工作流\
  ├── 01_目标定义.md        # 已有
  ├── 02_任务拆解.md        # 本文
  ├── harness/              # 引擎（后续 Phase 填充）
  ├── templates/            # 新工程模板
  ├── skills/               # 技能包
  └── .gitignore
  ```
- **验收**：目录结构符合预期，`git init` + 首次提交成功

### T0.2 环境探测 env_probe（S1）
- **目标**：自动探测工具链/调试器/板卡，输出 `harness env check` 所需信息
- **探测项**：
  - OpenOCD 可执行文件（多位置扫描）
  - arm-none-eabi-gcc（或 riscv/xtensa 编译器）
  - ST-Link / J-Link / CMSIS-DAP 调试器连接
  - Python 版本、Git
  - 平台识别（Windows/Linux/macOS）
- **产出**：`harness/core/env_probe.py` + 探测结果结构化输出
- **验收**：S1 —— `harness env check` 全部 OK

### T0.3 OpenOCD Registry 多分支管理（P3）
- **目标**：解决"STM32CubeIDE 内置 OpenOCD 换芯片不能用"问题——多分支共存
- **设计**：
  ```
  OpenOCD Registry = 扫描结果注册表
  ├── [st]      STM32CubeIDE 内置    标签: [stm32, stlink]
  ├── [esp32]   openocd-esp32        标签: [esp32, xtensa, riscv]
  └── [mainline] 官方主线 0.12+       标签: [generic, riscv, fpga]
  
  harness openocd list                # 查看已发现的分支
  harness openocd use <board>         # 按板卡 Profile 自动路由
  ```
- **产出**：`harness/core/openocd_registry.py`（扫描 + 登记 + 按能力标签匹配）
- **验收**：能发现 ST 分支；`harness openocd list` 输出包含 [st] 且路径正确

---

## Phase 1：抽象层核心引擎

### T1.1 调试会话抽象 SWDSession（S2, S3）
- **目标**：实现 MCP 风格调试会话（对齐 AixProbe 接口语义）
- **接口**：`connect / halt / resume / register_read / memory_read / memory_write / backtrace / chip_identify / read_fault`
- **强制前置**：所有硬件操作必须先 `connect()`，未连接返回 `"no active session"`
- **产出**：`harness/core/swd_session.py`（基于现有 harness_ai 的 SWDDebugSession 提炼）
- **验收**：S2（F407 探测出 CPUID=0x410FC241）+ S3（健康检查异常号 0）

### T1.2 双协议支持设计（P1, P2, S9）
- **目标**：抽象层接口不绑定 SWD/JTAG，协议切换只改配置
- **设计**：
  ```
  session.connect()  # 内部根据 Profile.transport 决定:
  │   transport = "swd"  → openocd cfg: dapdirect_swd
  │   transport = "jtag" → openocd cfg: transport select jtag
  接口不变，底层 openocd.cfg 由 Profile 提供
  ```
- **产出**：协议无关的会话接口 + 文档说明（如何新增一种架构）
- **验收**：S9 —— 代码评审：新增 MCU 仅加 Profile + cfg，不改核心引擎

### T1.3 诊断引擎 diagnostics（S5, S6）
- **目标**：HardFault/挂死/内存三类诊断 + 结构化报告
- **能力**：
  - HardFault：CFSR/HFSR/MMFAR/BFAR 位域 + 栈回溯 + ELF 源码映射
  - 挂死：5 次 PC 采样区分死循环/跑飞/中断风暴
  - 内存：SP 栈使用率 + 向量表 + FreeRTOS 堆 + canary
- **产出**：`harness/core/diagnostics.py`（基于 harness_ai 的 diagnosis.py 提炼，**架构无关化**）
- **验收**：S5（人为 HardFault 完整报告）+ S6（PC 采样四类判断）

### T1.4 烧录封装 flasher（S4）
- **目标**：多段烧录 + verify 校验，协议/分支无关
- **设计**：按 Profile 路由 OpenOCD 分支，支持 BL+APP+签名+参数多段 program verify
- **产出**：`harness/core/flasher.py`
- **验收**：S4 —— F407 4 段全部 Verified OK

### T1.5 测试断言引擎 tester
- **目标**：YAML 场景驱动，8 种断言
- **产出**：`harness/core/tester.py` + scenarios 模板
- **验收**：能加载场景 YAML 并执行断言（复用 harness_ai expectations.py）

---

## Phase 2：harness CLI 统一入口

### T2.1 CLI 框架（S1 落地）
- **目标**：`harness` 命令统一入口（argparse，子命令）
- **子命令**：`env check / init / build / flash / test / diagnose / openocd list`
- **产出**：`harness/harness_cli.py` + `harness/__main__.py`（支持 `python -m harness`）
- **验收**：全部子命令可调用，`harness --help` 完整

### T2.2 板卡 Profile 机制（P4, S9）
- **目标**：解决"VSCode 高度定制 STM32 换芯片难"——板卡即配置
- **Profile 结构**：
  ```json
  // boards/stm32f407/profile.json
  {
    "mcu": "stm32f407",
    "arch": "cortex_m4",
    "toolchain": "arm-none-eabi-gcc",
    "transport": "swd",
    "openocd": "st",
    "openocd_cfg": "openocd.cfg",
    "flash_sections": [
      {"file": "bootloader.elf", "addr": "0x08000000"},
      {"file": "MY_OTA_GUI.elf", "addr": "0x08010000"},
      {"file": "signature.bin",  "addr": "0x080DFF80"},
      {"file": "param_init.bin", "addr": "0x080E0000"}
    ]
  }
  ```
- **产出**：`boards/stm32f407/profile.json` + Profile 加载器
- **验收**：S9 —— 新增一块板卡 = 新增一个 profile.json + cfg，引擎零改动

### T2.3 VSCode 配置自动生成（P4）
- **目标**：launch.json / tasks.json 由 harness 按 Profile 生成，不手写
- **设计**：`harness vscode-gen <board>` → 生成 .vscode/launch.json（Cortex-Debug 或通用配置）+ tasks.json
- **产出**：`harness/core/vscode_gen.py`
- **验收**：对 stm32f407 Profile 生成可用 launch.json；切换 Profile 后重新生成

---

## Phase 3：MCP 服务封装

### T3.1 mcp_server.py（S7）
- **目标**：把引擎能力暴露为 MCP 标准工具
- **工具清单**：`connect / halt / resume / memory_read / memory_write / register_read / chip_identify / diagnose / build / flash / test`
- **产出**：`harness/mcp_server.py`
- **验收**：S7 —— 任一 AI 工具注册成功，AI 能调用 connect()

---

## Phase 4：技能包知识库

### T4.1 SKILL.md 路由（S8）
- **目标**：问题描述 → 自动路由到诊断流程
- **产出**：`skills/SKILL.md`（关键词映射 + 流程路由表）
- **验收**：S8 —— 新 AI 读 SKILL.md 能按问题路由

### T4.2 核心诊断技能文档
- **产出**：`skills/diagnosis-hardfault.md / diagnosis-hang.md / diagnosis-memory.md`（基于 AixProbe 技能包改编，遵守 CC BY-NC-SA）
- **验收**：每份技能含"流程 + 寄存器表 + 报告模板"

### T4.3 芯片参考 + SVD 目录
- **产出**：`skills/chip-reference.md`（F407/F103/ESP32-C3 基础表）+ `skills/svd/arm/` 目录
- **验收**：F407 SVD 就位，register-decode 流程可用

---

## Phase 5：模板与标准流程

### T5.1 新工程模板
- **产出**：`templates/boards/stm32f407/` + `templates/boards/stm32f103/`（含 CMake + 链接脚本 + openocd.cfg + profile.json）
- **验收**：`harness init demo` 生成可用工程骨架，能编译

### T5.2 STANDARD_PROCESS.md
- **产出**：标准流程完整版（12 步，含每步的"谁做/调什么/产出"）
  - ✅ **已由 `STANDARD_PROCESS.md` v0.2 实现**（本任务完成）
- **验收**：新 AI 读后能理解并执行完整流程

### T5.3 自引导蓝图修订
- **产出**：修订 `EMBEDDED_AI_WORKFLOW.md` 为最终版（对齐本次拆解）
- **验收**：蓝图与任务拆解无矛盾，S10 前提满足

---

## Phase 6：全量验收

### T6.1 真机验收（S1-S6）
- **操作**：F407 真机跑通 环境自检 → 连接 → 健康检查 → 烧录 → HardFault 诊断 → 挂死诊断
- **验收**：S1-S6 全部通过，形成验收记录

### T6.2 工具与通用性验收（S7-S9）
- **操作**：MCP 接入 AI 工具；代码评审新增 MCU 流程；技能包路由测试
- **验收**：S7-S9 通过

### T6.3 自引导模拟验证（S10）
- **操作**：模拟"新 AI 无对话上下文"场景，仅给工作流包，验证其能独立执行
- **验收**：S10 通过

---

## 二、依赖关系与执行顺序

```
T0.1 → T0.2 → T0.3
         ↓
T1.1 → T1.2 → T1.3 → T1.4 → T1.5
         ↓
T2.1 → T2.2 → T2.3
         ↓
T3.1
         ↓
T4.1 → T4.2 → T4.3
         ↓
T5.1 → T5.2 → T5.3
         ↓
T6.1 → T6.2 → T6.3
```

**可并行**：T4.x（技能包文档）与 T1.x（引擎）无代码依赖，可并行推进。

---

## 三、任务 ↔ 成功标准映射

| 成功标准 | 验收任务 |
|---------|---------|
| S1 环境自检 | T0.2, T2.1 |
| S2 连接硬件 | T1.1 |
| S3 健康检查 | T1.1 |
| S4 完整烧录 | T1.4 |
| S5 HardFault 诊断 | T1.3 |
| S6 挂死诊断 | T1.3 |
| S7 MCP 接入 | T3.1 |
| S8 技能包路由 | T4.1 |
| S9 抽象层通用性 | T1.2, T2.2 |
| S10 自引导 | T5.3, T6.3 |

---

## 四、执行建议

1. **从 T0.1 开始顺序执行**，每个任务完成即 git 提交
2. T1.x 引擎可直接从 harness_ai 提炼（已有验证代码），重点是"架构无关化"改造
3. T4.x 技能包可基于 AixProbe 技能包改编（遵守协议）
4. 每个 Phase 结束做一次真机验证，问题早暴露早修
