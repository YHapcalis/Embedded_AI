# 目标定义（Goal Definition）

> 文件：`01_目标定义.md`
> 用途：嵌入式 AI 工作流搭建项目的 **第 0 步交付物** —— 先定目标，后拆任务。
> 原则：本文件是所有后续决策（任务拆解、架构设计、验收）的**唯一依据**。
> 更新：任何目标变更必须回到本文件修订，不得口头改。

---

## 一、愿景（Vision）— 为什么做

打造一份"**可运行的嵌入式 AI 开发工作流包**"：
无论未来在什么开发环境、用哪个 AI 编程工具、开发哪块 MCU 芯片，
把这份工作流包交给任意 AI，它都能自行解读并开始搭建完整工作流，
实现 AI 对嵌入式开发的"需求 → 编码 → 编译烧录 → 验证 → 诊断修复 → 交付"闭环。

一句话：**让 AI 成为可迁移的嵌入式开发工程师，工作流是可携带的"岗位说明书+工具箱"。**

---

## 二、目标（Goal）— 要达成什么

在 `E:\嵌入式AI工作流\` 目录下，交付一份**可运行的工作流包**，包含：

1. **自引导蓝图**：一份规范文档，任意 AI 读取后能理解全貌并按流程执行
2. **通用抽象层引擎**：实现硬件操作、编译烧录、测试验证、诊断修复的核心代码（不绑定具体 MCU）
3. **MCP 服务**：把引擎能力暴露为标准工具接口，任意支持 MCP 的 AI 工具可接入
4. **技能包知识库**：AI 诊断问题所需的知识（路由 + 诊断流程 + 芯片参考 + SVD）
5. **标准流程文档**：AI 执行开发任务时遵循的 8 步流程

**目标的可验证表述**：新 AI 拿到本工作流包后，无需任何额外说明，
即可完成"从需求到交付"的嵌入式开发全流程（以 F407 作为参考验证平台）。

---

## 三、范围（Scope）

### 3.1 包含（In-Scope）
- 通用硬件抽象层（调试会话、内存/寄存器读写、芯片探测、诊断引擎）
- 编译烧录封装（build/flash，支持 verify 校验）
- 测试断言引擎（YAML 场景驱动，8 种断言）
- MCP 服务封装（connect/halt/read/diagnose/build/flash/test）
- 技能包知识库（SKILL.md 路由 + 核心诊断技能 + 芯片参考 + SVD 目录）
- 标准流程文档（STANDARD_PROCESS.md，8 步）
- 新工程模板（harness init，多板卡骨架）
- 环境探测与配置管理（跨平台 Windows/Linux/macOS）
- F407 参考验证（作为抽象层的真机验收平台）

### 3.2 排除（Out-of-Scope）
- 不绑定具体 MCU 型号的专用逻辑（芯片适配由用户按抽象层接口自行补充）
- 不开发 GUI/IDE 插件（仅 CLI + MCP + 技能包）
- 不做云端/远程调试（本地调试为主，远程为可选扩展）
- 不实现商业级 CI/CD 平台（脚本化即可）
- 不包含摄像头/传感器等具体外设的驱动实现

### 3.3 边界（Boundary）
- 抽象层接口稳定后冻结；新增 MCU 只加"适配文件"，不改核心引擎
- 技能包知识遵循"诊断逻辑与芯片知识分离"：换 MCU 加 chip-reference + SVD
- 工作流包本身可 git 版本化，随项目演进

---

## 四、成功标准（Success Criteria）— 怎么算完成

> 每一条都可独立验证，不依赖主观判断。

| # | 验收项 | 判定方式 | 验收平台 |
|---|--------|---------|---------|
| S1 | 环境自检通过 | `harness env check` 输出工具链/调试器/板卡全部 OK | 本机 |
| S2 | 抽象层可连接硬件 | `connect()` 返回 arch/target，芯片探测出 F407（CPUID=0x410FC241, DEV_ID=0x413） | F407 真机 |
| S3 | 健康检查 | halt 后读寄存器正常，异常号=0，CFSR=0 | F407 真机 |
| S4 | 完整烧录 | 4 段（BL+APP+签名+参数）program verify 全部 Verified OK | F407 真机 |
| S5 | 诊断能力 | 人为触发 HardFault，诊断引擎输出：异常类型+CFSR位域+栈回溯崩溃PC+源码定位 | F407 真机 |
| S6 | 挂死诊断 | 5 次 PC 采样能区分死循环/跑飞/正常运行 | F407 真机 |
| S7 | MCP 可接入 | 任一 AI 工具（Claude Code/Trae）注册 MCP 成功，AI 能调用 connect() | 本机 |
| S8 | 技能包可路由 | 新 AI 读 SKILL.md 后能按"问题→诊断流程"自动路由 | 本机 |
| S9 | 抽象层通用性 | 在不改核心引擎的前提下，可声明新增一种 MCU（仅加适配文件） | 代码评审 |
| S10 | 自引导完整性 | 新 AI 仅凭工作流包（无对话上下文）能独立执行标准流程 | 模拟验证 |

**验收节奏**：每个阶段交付后，按上表相关项真机验收，通过才进入下一阶段。

---

## 五、约束（Constraints）

| 类型 | 约束 |
|------|------|
| 协议 | AixProbe 技能包为 CC BY-NC-SA 4.0（非商用）：可学习借鉴，不可商用、不可冒充原创 |
| 环境 | Windows（当前）/ Linux / macOS 兼容；Python 3.10+ |
| 工具链 | OpenOCD + arm-none-eabi-gcc（可探测）；调试器 ST-Link/J-Link/CMSIS-DAP 均可 |
| 硬件 | 参考验证平台：STM32F407ZGT6；抽象层不得绑定该型号 |
| 诚实 | 工具不可用时如实报告，不假装成功；简历/面试不夸大 |
| 安全 | 硬件操作设上限（默认 15 次/会话）；AI 修改代码受文件白名单约束 |

---

## 六、干系人（Stakeholders）

| 角色 | 是谁 | 期望 |
|------|------|------|
| 使用者 | 未来的自己（开新项目时） | 丢给新 AI 即可用，不重复搭建 |
| 执行者 | 未来的新 AI | 读蓝图能独立搭建并运行工作流 |
| 验收者 | 现在的自己（真机） | 每次交付 F407 实测通过 |
| 受益者 | 面试官视角的求职者 | 可讲述"嵌入式 AI 工作流标准化"的故事 |

---

## 七、后续（Next Steps）

> 目标定义完成 ✅ → 下一步是**任务拆解**（Task Breakdown），
> 基于本文件的三、范围 与 四、成功标准，将工作流包拆解为可执行的任务清单。
> 任务拆解文档见：`02_任务拆解.md`（待创建）

---

*本文件为项目第 0 步交付物，任何目标相关变更必须先修订本文件。*
