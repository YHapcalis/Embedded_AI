# STM32F103C8T6 — ST-Link (SWD) 烧录/调试配置
# 供 foundation flasher 使用（脚本目录由 Registry 自动带出 -s st_scripts）:
#   openocd -s <st_scripts> -f <本文件> -c "program <elf> verify" -c "reset; exit"
source [find interface/stlink.cfg]
source [find target/stm32f1x.cfg]
reset_config srst_only
