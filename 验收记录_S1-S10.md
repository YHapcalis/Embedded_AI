# Phase 6 全量验收记录（S1-S10 全部通过）

> 日期：2026-08-28 ｜ 平台：STM32F407ZGT6 + ST-Link V2 + OpenOCD 0.12（ST 分支）
> 结果：**S1-S10 全部 PASS** —— 嵌入式 AI 工作流搭建完成 ✅

---

## 验收结果总表

| # | 成功标准 | 结果 | 关键证据 |
|---|---------|------|---------|
| S1 | 环境自检 | ✅ PASS | `env check` 6/6 OK（openocd/gcc/stlink/python/git/platform） |
| S2 | 连接+芯片探测 | ✅ PASS | `connect()` → CPUID=0x410FC241, DEV_ID=0x413, Flash=1024KB |
| S3 | 健康检查 | ✅ PASS | halt 后 CFSR=0，无 Fault |
| S4 | 完整烧录 | ✅ PASS | 4 段（BL+APP+签名+参数）全部 Verified OK |
| S5 | HardFault 诊断 | ✅ PASS | 异常号3 + CFSR UNDEFINSTR + FORCED + 栈回溯崩溃PC 0x080DFF8A + 源码定位 lv_event.c:77 |
| S6 | 挂死诊断 | ✅ PASS | 5 次 PC 采样 + FreeRTOS 源码定位 tasks.c |
| S7 | MCP 接入 | ✅ PASS | 标准 MCP 协议调用 chip_identify 返回正确 |
| S8 | 技能包路由 | ✅ PASS | SKILL.md 路由完整，11 个技能文件就位 |
| S9 | 抽象层通用性 | ✅ PASS | 引擎逻辑零 MCU 绑定，2 板卡 Profile 隔离，知识分离到 Profile |
| S10 | 自引导完整性 | ✅ PASS | 蓝图+流程+CLI+技能+模板五件套自包含 |

---

## 验收中发现的 4 个问题（全部已修复）

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 烧录 Verify Failed | ELF 加了显式 addr，OpenOCD 报 'no flash bank for 0x1000xxxx' | ELF 不加 addr（用 ELF 内部地址），bin 才加（提交 7c1e2c6） |
| 2 | flasher 结果误判 FAIL | 解析用绝对路径匹配失败 | 改为统计 Verified OK 计数（同提交） |
| 3 | MCP chip_identify 失败 | 过时 import `core.swd_session` | 改为 `core.session`（提交 5234d5e） |
| 4 | 知识硬编码引擎 | 内存布局在 diagnostics.py 字典 | 移到 Profile memory 字段（提交 1eda0f1） |

---

## 技术亮点（面试可讲）

1. **foundation 引擎独立复现 harness_ai 全部诊断能力**：HardFault 栈回溯 + ELF 源码定位在真机输出与 harness_ai 完全一致
2. **架构无关性真实验证**：同一套引擎代码驱动 F407（ARM/SWD）和 ESP32-C3（RISC-V/JTAG）两块板卡
3. **MCP 标准协议打通**：任何支持 MCP 的 AI 工具可直接驱动硬件调试
4. **诊断知识分层**：芯片知识（内存布局/ID 表）与诊断逻辑分离，换 MCU 只加 Profile + chip-reference

---

## 遗留事项

| 事项 | 说明 |
|------|------|
| SVD 扩展 | 目前仅 F407，ESP32/RISC-V 的 SVD 可后续补充 |
| 定时回归 | 工作流支持，但未配置自动触发（可用 WorkBuddy 自动化实现） |
| 真实新项目演练 | S10 是模拟验证，建议用真实新项目完整走一遍 12 步流程 |
| HangDiagnosis 分类局限 | RTOS 空载时采样集中 idle 任务会被判"死循环"，无法与真死循环区分 |
| ELFTools PATH 兜底 | arm-none-eabi-addr2line 不在 PATH 时静默降级为纯地址，应像 env_probe 一样兜底扫描 |

---

## 复现附录（2026-08-29 独立验证）

> 由另一 AI 会话在不依赖本记录结论的前提下独立复现，**S1-S10 全部 PASS**。
> 引擎修复随之提交：`8a34505 fix(engine)`。

### 复现结果

| # | 结果 | 独立采到的证据 |
|---|------|--------------|
| S1 | ✅ | `env check` 6/6 OK |
| S2 | ✅ | CPUID=0x410FC241、DEV_ID=0x413（0xE0042000=0x100F6413）、Flash=0x0400=1024KB（0x1FFF7A22，**需先 reset run 才能读对**） |
| S3 | ✅ | halt 后 xPSR=0x81000000（异常号 0）、CFSR=0x00000000、psp=0x20009090（FreeRTOS） |
| S4 | ✅ | 4 段烧录 Verified OK，**复跑两遍均成** |
| S5 | ✅ | PC 注入签名区(0x080DFF81) + HardFault_Handler(0x080409AA) 入口硬件断点 → 异常号 3、HFSR FORCED、CFSR=0x8200、崩溃点 0x080DFF88、源码定位 stm32f4xx_it.c:119 |
| S6 | ✅ | 5 次 PC 采样 + FreeRTOS 源码定位（tasks.c prvIdleTask） |

### 口味差异说明（如实记录）

- S5 记录原文为 **UNDEFINSTR**，复现为 **PRECISERR(BusFault)**：注入执行流在
  签名数据里跑的步数不同，故障口味随之不同。S5 验收本质（输出完整诊断
  报告：类型+位域+回溯+源码）不受影响。
- S6 结论"死循环（PC 高度集中）"实为**板子空载、采样集中 idle 任务**——
  分类器无法区分"RTOS 空闲"与"真死循环"，已列入遗留事项。

### 复现发现的新机制（已固化进引擎）

1. **板上固件 IWDG 在跑**（RCC_CSR IWDGRSTF 实锤）：运行时正常喂狗
   （20s 纯旁观零复位），但 **fault 后 CPU 停止喂狗，IWDG 在
   "fault → 诊断 halt"窗口内复位芯片** → CFSR 被清 + OpenOCD 失步
   （targets 卡 reset、halt 永远超时）。原验收能过 = 诊断抢在窗口内。
   → 引擎新增 `session.freeze_watchdogs()`，三类诊断自动调用
2. **冷启动必须注入 `-s`**：ST 分支 scripts 库在
   `mcu.debug.openocd` 插件 `resources/openocd/st_scripts/`，
   旧版 `find_openocd()` 丢弃 `script_dir` 导致冷启动必挂
3. **坏实例防御**：遗留坏 OpenOCD 占 6666 端口时，旧检查会误报
   "ST-Link 已连接"（实际全是缓存值）→ 已升级为"读 CPUID 验链路"
