# 项目上下文文档

> 本文件是 AI 团队了解工程全貌的**唯一入口**（STANDARD_PROCESS ③ 读取 / ⑫ 更新）。
> 每次任务完成后必须更新本文件，让未来的 AI 和人类能快速接手。
> 模板版本：v1.0 ｜ 使用：复制本文件到项目根目录，重命名为 `PROJECT_CONTEXT.md` 并填写。

---

## 1. 项目概况

| 项 | 内容 |
|----|------|
| 项目名称 | （如：MY_OTA_GUI 智能仪表盘） |
| 目标 | （一句话：这个项目做什么） |
| 硬件平台 | （MCU 型号 / 开发板 / 关键外设） |
| 软件栈 | （RTOS / GUI / 协议栈 / 工具链） |
| 构建方式 | （CMake / Make / IDE） |
| 调试器 | （ST-Link / J-Link / CMSIS-DAP / 板载） |

## 2. 目录结构

```
<项目根>/
├── Core/
│   ├── Inc/          # 头文件
│   └── Src/          # 源码
├── Drivers/          # 厂商驱动
├── build/            # 构建产物
├── docs/             # 文档
└── ...（按实际填写）
```

| 目录/文件 | 职责 |
|-----------|------|
| （路径） | （该模块做什么） |
| （路径） | （该模块做什么） |

## 3. 构建系统

| 项 | 内容 |
|----|------|
| 构建命令 | （如：`cmake --build build/Debug --parallel 8`） |
| 产物路径 | （如：`build/Debug/MY_OTA_GUI.elf`） |
| 烧录命令 | （如：`foundation flash stm32f407`） |
| 烧录段 | （BL@地址 / APP@地址 / 签名@地址 / 参数@地址） |
| 特殊配置 | （链接脚本 / 签名流程 / 多区布局） |

## 4. 引脚与外设映射

| 引脚 | 功能 | 外设 | 说明 |
|------|------|------|------|
| （如 PA9） | （如 USART1_TX） | （如 USART1） | （备注） |
| （如 PC6） | （如 DCMI_D0） | （如 DCMI） | （备注） |

| 通信接口 | 协议 | 速率 | 用途 |
|---------|------|------|------|
| （如 CAN1） | （如 CAN 2.0B） | （如 500kbps） | （如 传感器数据） |

## 5. 功能模块清单

| 功能 | 关键文件 | 入口函数 | 状态 |
|------|---------|---------|------|
| （如 主界面） | （如 Core/Src/app_ui.c） | （如 app_ui_create()） | ✅ 完成 / 🚧 开发中 |
| （功能） | （文件） | （入口） | 状态 |

## 5.1 架构声明（AI 进入工程时填写，遵守 embedded-engineering-rules）

| 文件 | 四层归属 | 设计模式 | 理由 |
|------|---------|---------|------|
| （如 main.c） | APPLICATION | 状态模式（主） | 任务编排+状态切换 |
| （如 canif.c） | DRIVER | — | 外设芯片驱动 |
| （文件） | （层） | （模式） | （理由） |

> 主模式 1 个 + 辅模式 0-2 个，匹配理由见 embedded-engineering-rules.md。

## 6. 调试信息

| 项 | 内容 |
|----|------|
| OpenOCD 配置 | （如：`openocd.cfg`，ST-Link SWD） |
| 调试入口 | （如：APP @ 0x08010000） |
| 关键地址 | （如：外部 SRAM 0x68000000，draw_buf @ 0x68010000） |
| 诊断工具 | （如：`foundation diagnose hardfault --elf ...`） |
| 特殊调试技巧 | （如：resume[map_addr] 调固件函数，页面函数不可 halt） |

## 7. 历史踩坑记录

| 日期 | 问题 | 根因 | 解法 | 相关文件 |
|------|------|------|------|---------|
| （日期） | （现象） | （根因） | （解法） | （文件） |
| 2026-08-28 | 仪表盘花屏 | FSMC AddressSetupTime=0 时序不足 | 改为 1 + BusTurnAround=1 | fsmc.c |

> 这是团队最宝贵的资产——新 AI 先读这里，避免重复踩坑。

## 8. 未完成事项与限制

| 事项 | 类型 | 说明 |
|------|------|------|
| （待办） | TODO / 已知限制 / 扩展方向 | （说明） |

---

*本文件随项目演进持续更新。每次任务完成后（STANDARD_PROCESS ⑫），AI 必须回到本文件更新对应章节。*
