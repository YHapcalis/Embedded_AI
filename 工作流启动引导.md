# 工作流启动引导 — 嵌入式 AI 开发工作流 · 启动引导

> **本文件是"启动引导"（Bootstrapper）——只包含你必须先知道的最小信息。**
> 其余知识**按需加载**（渐进式披露），不要一口气读完所有文件。
>
> 版本：v3.0 ｜ 更新：2026-08-28 ｜ 作者：Ryumeido Mei（余豪）
> v3.0 变更：**渐进式披露改造**（Token 经济性优化）

---

## 0. 给 AI 的第一指令（必读）

你是嵌入式 AI 开发工作流的**使用者/维护者**。

**你的任务**：
1. 只读本文件（约 300 tokens），了解工作流全貌
2. 按任务需要**按需加载**对应文件（见下方"渐进式披露规则"）
3. 使用 `foundation/` 的 CLI 和 MCP 服务操作开发板
4. 涉及硬件操作/烧录前，先向用户确认

**⚠️ 渐进式披露规则（Token 经济性，必须遵守）：**
- ❌ **禁止**一次性读入所有 Markdown 文档（浪费 token）
- ❌ **禁止**直接读取 `skills/svd/` 下的 .svd 文件全文（519K tokens！）
- ✅ **按需加载**：执行哪个流程，才读哪个文件（见目录索引）
- ✅ **Grep 检索**：查寄存器/芯片信息用 Grep 搜索，不整读文件
- ✅ 每步任务完成，只保留任务相关的上下文

---

## 1. 渐进式披露加载规则（核心）

### 什么时候读什么（Just-in-Time 原则）

| 任务 | 才读的文件 | 大小 |
|------|-----------|------|
| 启动/了解全貌 | **本文件**（你正在读的） | ~300 tokens |
| 开始执行开发流程 | `标准流程12步.md`（12 步流程） | ~1.7K |
| **进入嵌入式工程/写代码前** | **`foundation/templates/embedded-engineering-rules.md`（工程宪法）** | ~2K |
| 诊断问题 | `skills/SKILL.md`（路由）→ 命中后只读对应 `diagnosis-*.md` | ~660 + 单个 |
| 查芯片型号 | `skills/chip-reference.md`（用 Grep 按型号搜，含 CKS32 国产替代段） | 按需行 |
| 查寄存器定义 | `skills/svd/`（**用 Grep 搜外设名，绝不整读**） | 按需行 |
| CKS 系列开发/踩坑 | `skills/cks-pitfalls.md`（**仅 CKS 芯片任务读**：重映射断调试口/野地址 HardFault 等） | ~1.2K |
| 新项目骨架 | `templates/boards/stm32f407/` | ~1K |
| 交付文档 | `foundation/templates/delivery-report.md` | ~400 |
| 编码规范补充 | `foundation/templates/coding-standard.md`（rules 的补充，命名/commit 用） | ~300 |
| 项目上下文 | `PROJECT_CONTEXT.md`（若有） | 项目相关 |

> **⚠️ 约束层铁律**：进入嵌入式工程后、写任何代码前，
> **必须先读 `embedded-engineering-rules.md`**（四层架构/六模式/ISR/内存/CubeMX 边界）。
> 它比 coding-standard 优先级更高——冲突时以它为准。

### SVD 文件的铁律（519K tokens 的炸弹）

```
❌ 禁止: Read skills/svd/arm/STM32F407xx.svd  （一次性读入 = 爆上下文）
✅ 正确: Grep("RCC_CR", glob="*.svd")          （只取匹配行）
✅ 正确: Grep("USART.*CR1", glob="*.svd", "-i")
```

> SVD 是"字典"，不是"文章"。用查的方式，不用读的方式。

---

## 2. 目录索引（哪个文件是干嘛的）

```
E:\嵌入式AI工作流\
├── 工作流启动引导.md          ← 本文件（启动引导，仅此一页）
├── 标准流程12步.md         ← 12 步流程（开始干活才读）
├── docs/
│   └── 设计决策与约束.md      ← 设计决策+协议约束（⚠️ 非执行必读，维护者用）
├── foundation/                ← 引擎（代码，按模块引用）
│   ├── cli/main.py            ← CLI 入口（python -m foundation）
│   ├── mcpservice/server.py   ← MCP 服务（8 工具）
│   ├── core/                  ← 引擎模块（session/diagnostics/...）
│   └── templates/             ← 模板（上下文/编码规范/交付）
├── boards/                    ← 板卡 Profile（stm32f407/esp32c3/stm32f103/esp32s3/cks32f103/cks32f105）
├── templates/boards/          ← 工程模板
├── skills/                    ← 技能包（SKILL.md 路由 + 诊断技能）
│   ├── cks-pitfalls.md        ← CKS 国产替代踩坑速查（⚠️ 仅 CKS 任务读）
│   └── svd/                   ← SVD 描述（⚠️ 仅 Grep 检索用）
└── 验收记录_S1-S10.md         ← 验收证据（S1-S10 全过 + 2026-08-29 独立复现附录）
```

---

## 3. 快速开始（3 条命令）

```bash
python -m foundation env check          # 环境自检
python -m foundation openocd list       # OpenOCD 多分支
python -m foundation chip-confirm --mcu stm32f407 --arch cortex_m4  # 确认芯片
```

---

## 4. 安全规则（AI 必须遵守）

1. **硬件操作安全阀**：烧录/复位/写寄存器前先确认；操作上限 15 次/会话
2. **文件白名单**：只能修改任务允许的文件（team_orchestrator 强制）
3. **HardFault 检测**：每次硬件操作后检查，异常立即停止并诊断
4. **诚实原则**：工具不可用时如实报告，不假装执行成功
5. **渐进式披露**：按需加载，不整读大文件（尤其 SVD）

---

## 5. 当前状态（一句话）

工作流已搭建完成并通过真机验收（S1-S10 全过）。
需要细节？按需读对应文件即可——不必读完整个仓库。

---

*本引导页刻意精简。任何"完整文档"都在独立文件里，用才读。*
