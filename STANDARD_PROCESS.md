# 标准流程（STANDARD_PROCESS）— v0.3

> 文件：`STANDARD_PROCESS.md`
> 用途：AI 团队执行嵌入式开发任务时遵循的完整流程（12 步）
> 状态：**定稿**（经真机验收 S1-S10 通过）
> **⚠️ 渐进式披露**：本文件在执行流程时才读（~1.7K tokens）。
>   已了解全貌的 AI（读完蓝图 v3.0）可在执行第 N 步时只参考对应步骤段落。
> 版本历史：
>   - v0.3（2026-08-28）：标注渐进式披露；状态从草案转定稿
>   - v0.2（2026-08-28）：① 环境检查前新增"确认目标芯片类型"前置步骤
>   - v0.1（2026-08-28）：初稿，融合用户 8 步流程 + 评审补缺

---

## 〇、流程总览

```
┌─ 准备阶段（AI 独立完成）─────────────────────────────┐
│  ① 确认目标芯片 + 环境检查（验收门）                  │
│  ② 技能包 + MCP + 编码规范                          │
│  ③ 项目上下文 + 原理图                              │
│  ④ 拉起 AI 团队（角色 + 能力边界）                   │
└────────────────────────────────────────────────────┘
┌─ 规划阶段（人类敲定）───────────────────────────────┐
│  ⑤ 理解需求                                        │
│  ⑥ 风险与约束识别                                  │
│  ⑦ 目标定义 + 验收标准（★ 人敲定）                  │
│  ⑧ 任务拆解（映射验收标准）                         │
└────────────────────────────────────────────────────┘
┌─ 执行阶段（AI 团队 + 人类兜底）──────────────────────┐
│  ⑨ 团队干活（主理人管理）                           │
│  ⑩ 自主验收（断言可量化 + 终止条件）                 │
└────────────────────────────────────────────────────┘
┌─ 交付阶段──────────────────────────────────────────┐
│  ⑪ 交付文档                                        │
│  ⑫ 更新交接文档（闭环回 ③）                        │
└────────────────────────────────────────────────────┘

贯穿全程的安全横切面：
  [S1] 文件白名单   —— AI 只能修改允许的文件
  [S2] 硬件操作上限 —— 15 次/会话，防烧板
  [S3] HardFault 检测 —— 每次硬件操作后检查
  [S4] 诚实原则     —— 工具不可用如实报告，不假装成功
```

---

## 一、准备阶段（AI 独立完成）

### ① 确认目标芯片 + 环境检查（验收门）

**目的**：为 AI 搭好"手脚架"，确保能接触并操作开发板。
**前置**：环境检查之前，**先确认目标芯片类型**——它决定了需要哪种开发环境。

**执行**：

**第 1 步：询问目标芯片类型**
```
主理人向人类确认（或从需求/项目文档中提取）：
  · MCU/SoC 型号        （如 STM32F407ZGT6 / ESP32-C3 / CH32V003 ...）
  · 内核架构            （Cortex-M4 / RISC-V / Xtensa ...）
  · 调试接口            （SWD / JTAG / 板载 USB-JTAG ...）
```
> 芯片类型决定后续一切：OpenOCD 分支、编译器、调试协议、SVD 文件。
> 不确定型号时，可先让 AI 尝试 `connect()` 自动探测，再与人类确认。

**第 2 步：判断所需开发环境（按芯片类型映射）**

| 芯片类型 | OpenOCD 分支 | 编译器 | 调试协议 |
|---------|-------------|--------|---------|
| STM32 / GD32(ARM) / NXP Cortex-M | ST 分支（或主线） | arm-none-eabi-gcc | SWD（默认）/ JTAG |
| ESP32 / S2 / S3（Xtensa） | Espressif 分支 | xtensa-esp32-elf-gcc | JTAG |
| ESP32-C3/C6、CH32V（RISC-V） | Espressif 分支 / 主线 | riscv32-esp-elf-gcc | JTAG |
| 其他 RISC-V / FPGA | 主线（generic） | 对应厂商工具链 | JTAG |

**第 3 步：补全缺失组件**
```
harness openocd list      # 检查所需分支是否已注册
harness env check         # 检查编译器/调试器
发现缺失 → 提示人类安装（或自动补全）：
  · OpenOCD 分支缺失 → 提示安装对应分支（不覆盖现有）
  · 编译器缺失 → 提示安装对应工具链
  · 补全完成后重新检查
```

**第 4 步：验收门**
以下全部通过才进入下一步，否则**先报告环境问题**（不假装成功）：
- [ ] 已确认目标芯片类型（型号 + 架构 + 调试接口）
- [ ] 所需 OpenOCD 分支可用
- [ ] 所需编译器可用（对应架构）
- [ ] 调试器连接（ST-Link/J-Link/CMSIS-DAP/板载）
- [ ] 板卡可连接（connect 成功 + 芯片识别）

**产出**：芯片类型确认记录 + 环境检查报告（组件清单 + 版本 + 路径）

---

### ② 技能包 + MCP + 编码规范

**目的**：为 AI 装上"大脑"，获得领域知识和硬件操作能力。

**执行**：
1. 加载技能包（`skills/` 目录）：SKILL.md 路由 + 诊断技能 + 芯片参考 + SVD
2. 接入 MCP 服务：`harness mcp_server.py` 暴露的标准工具
3. **明确编码规范**（显式化，非模糊的"规范格式"）：
   - 语言标准（如 C11 子集 / MISRA-C 可选子集）
   - 命名规则（函数/变量/宏/文件）
   - 文件头模板（作者/日期/说明）
   - 错误处理约定（返回值 vs 断言 vs 异常）
   - 禁止模式（硬编码地址？直接访问外设寄存器的方式？）

**产出**：编码规范声明（写入项目文档，团队全员遵守）

---

### ③ 项目上下文 + 原理图

**目的**：让 AI 团队站在"前任工程师的肩膀上"，了解项目全貌，不重复造轮子。

**执行**（读取并消化）：
1. **项目上下文文档**（第 ⑫ 步的产物，若存在）：
   - 代码结构（目录树 + 模块职责）
   - 构建系统（CMake/Make 配置要点）
   - 引脚/外设映射表
   - 已有功能模块清单
   - 历史踩坑记录
2. **原理图**（若提供）：
   - 提取芯片型号、引脚连接、外设接口
   - 建立"引脚 → 功能"映射
3. 现有代码快速扫描（`harness` 辅助定位关键文件）

**产出**：项目理解摘要（AI 团队共享）

---

### ④ 拉起 AI 团队

**目的**：组建"主理人 + 专业成员"的协作结构。

**执行**：
1. **主理人**（Lead）：负责整体把控 —— 分配任务、审查产出、决定返工、对外沟通
2. **专业成员**（按任务定角色）：
   - 驱动工程师（外设/中断/DMA）
   - UI/应用工程师（LVGL/界面逻辑）
   - 通信工程师（CAN/UART/SPI/I2C）
   - 测试工程师（断言场景/验收）
   - （角色数量与分工由任务复杂度决定）
3. **每个成员声明能力边界**：
   - 负责哪些模块/文件
   - 可修改的文件白名单（结合安全横切面 S1）
   - 与其他成员的接口约定

**产出**：团队组建清单（角色 → 成员 → 职责 → 边界）

---

## 二、规划阶段（人类敲定）

### ⑤ 理解需求

**目的**：确保 AI 团队准确理解"要做什么"。

**执行**：
1. 主理人向人类确认需求细节（目标、背景、优先级）
2. 专业成员从各自领域提出疑问（可行性/影响面）
3. 汇总形成"需求理解确认单"，交人类确认

**产出**：需求理解确认单

---

### ⑥ 风险与约束识别

**目的**：提前识别可能阻碍任务的风险，避免执行中卡壳。

**执行**：
1. 技术风险：不熟悉的外设/协议/芯片特性
2. 环境风险：工具链缺失、版本不兼容
3. 硬件风险：无硬件验证项（如未接的传感器）
4. 约束盘点：协议限制（如 CC BY-NC-SA）、时间、硬件条件
5. 明确"AI 无法独立解决时呼叫人类的触发条件"

**产出**：风险与约束清单

---

### ⑦ 目标定义 + 验收标准（★ 人类敲定）

**目的**：在拆任务之前，先定"怎么算完成"—— 这是流程最重要的环节。

**执行**（主理人起草，人类拍板）：
1. 目标定义六要素：
   - 愿景（为什么做）
   - 目标（达成什么，可验证描述）
   - 范围（做什么 / 不做什么 / 边界）
   - **成功标准（怎么算完成 —— 可量化验收项）**
   - 约束（时间/环境/硬件/协议）
   - 干系人（谁受益）
2. **人类确认**：目标 + 范围 + 成功标准全部敲定，任何变更需回改

**验收标准示例**（可量化，非主观）：
- 编译零错误零警告（或警告阈值内）
- 断言通过率 100%（指定场景）
- 无 HardFault（CFSR=0）
- uwTick 心跳正常
- 指定功能在真机可操作验证

**产出**：目标定义文档（含验收标准）—— 人类签字版

---

### ⑧ 任务拆解（映射验收标准）

**目的**：把目标拆成可执行、可验收的任务。

**执行**：
1. 主理人把目标拆为任务清单（每个任务：描述/依赖/负责成员/验收项）
2. 每个任务映射到至少一条验收标准（保证"拆的任务都服务于验收"）
3. 人类确认任务清单与计划

**产出**：任务清单（任务 → 依赖 → 负责人 → 验收映射）

---

## 三、执行阶段（AI 团队 + 人类兜底）

### ⑨ 团队干活（主理人管理）

**目的**：按计划执行任务，主理人管控质量。

**执行**：
1. 主理人按依赖顺序分配任务给成员
2. 成员并行/串行执行（受文件白名单约束）
3. 主理人审查每个成员的产出：
   - 是否符合编码规范（②）
   - 是否在能力边界内
   - 是否满足对应验收项
   - 不合格 → 打回返工
4. **呼叫人类的触发条件**（⑥ 中已定义）：
   - AI 连续多次无法解决同一问题
   - 需要人类决策（方案取舍/范围变更/硬件操作）
   - 遇到环境问题无法自动解决

**安全横切面**：全程生效（白名单/操作上限/HardFault 检测/诚实）

**产出**：各任务完成记录 + 代码变更

---

### ⑩ 自主验收（断言可量化 + 终止条件）

**目的**：验证"真的完成了"，而非"看起来完成了"。

**执行**（循环，有终止条件）：
1. 自动编译烧录（`harness build` + `harness flash`）
2. 自动操作板卡功能验收（`harness test` + SWD 操作模拟）
3. 断言检查（可量化：通过率 100%、无 HardFault、心跳正常）
4. 发现 bug → 诊断引擎定位（`harness diagnose`）→ 修复 → 重新验收
5. **终止条件**（防死循环）：
   - 迭代上限：默认 15 轮（可配置）
   - 超时上限：总时长（可配置）
   - 连续 N 次（如 5 次）同样失败 → 判定"无法自动解决"
   - 触发终止 → 停止并呼叫人类，如实报告进展与卡点

**产出**：验收报告（断言结果/测试日志/问题清单）

---

## 四、交付阶段

### ⑪ 交付文档

**目的**：输出完整的交付物，供人类审阅和后续使用。

**执行**：
1. 固件产物（ELF/hex/bin + 烧录记录）
2. 源码变更说明（git diff + commit 记录）
3. 设计说明（模块架构/接口/时序）
4. 测试报告（断言结果 + 覆盖率）
5. 使用手册（烧录/配置/操作）
6. 变更日志（Changelog）

**产出**：交付文档包

---

### ⑫ 更新交接文档（闭环回 ③）

**目的**：让未来的 AI 和人类能快速了解工程全貌 —— 这是流程的闭环点。

**执行**：
1. 更新**项目上下文文档**（③ 读的文档）：
   - 最新代码结构
   - 新增/变更的模块
   - 更新的引脚/外设表
   - 新增踩坑记录（本任务遇到的问题 + 解法）
   - 未完成事项 / 已知限制
2. 更新标准流程文档（如流程本身有改进）

**产出**：更新版项目上下文文档（供下一任务/新 AI 读取）

---

## 五、安全横切面（贯穿全程，非某个步骤）

| 编号 | 机制 | 说明 |
|------|------|------|
| S1 | 文件白名单 | AI 只能修改场景/任务允许的文件；禁止改启动文件/链接脚本 |
| S2 | 硬件操作上限 | 默认 15 次/会话，防 AI 反复操作烧坏板子 |
| S3 | HardFault 检测 | 每次硬件操作后检查，异常立即停止并诊断 |
| S4 | 诚实原则 | 工具不可用如实报告；不假装编译/烧录/测试成功 |

---

## 六、角色与职责速查

| 角色 | 职责 | 何时介入 |
|------|------|---------|
| 人类（你） | 拍板目标/计划/验收标准；兜底无法解决的问题 | ⑦ 敲定、⑨ 被呼叫、⑩ 终止后 |
| 主理人 AI | 团队管理：分配/审查/返工决策；对外沟通 | 全程 |
| 专业成员 AI | 按边界执行具体任务 | ⑨ 干活 |
| 工作流（harness） | 环境/编译/烧录/测试/诊断工具 | ① ② ⑩ |

---

## 七、与 8 步原版流程的对应（变更记录）

| 原 8 步 | 修订后 | 变更 |
|---------|--------|------|
| 1 环境检查 | ① 确认芯片 + 环境检查 | 加"验收门" + **前置"确认目标芯片类型"**（芯片决定 OpenOCD 分支/编译器/协议，先确认再补全） |
| 2 技能包+MCP | ② 技能包+MCP+编码规范 | 编码规范显式化 |
| 3 上下文+原理图 | ③ 项目上下文+原理图 | 补充上下文文档内容清单 |
| 4 拉起团队 | ④ 拉起团队 | 加"角色按任务定 + 能力边界" |
| 5 需求+拆任务 | ⑤⑥⑦⑧ | **拆成 4 步**：需求→风险→目标定义(人敲定)→任务拆解 |
| 6 团队干活 | ⑨ 团队干活 | 加"主理人审查/返工"机制 |
| 7 自主验收 | ⑩ 自主验收 | 加"终止条件"防死循环 |
| 8 交付+交接 | ⑪⑫ 交付+交接 | 加"闭环回③" |

---

*本文件为草案 v0.1，待人类评审后定稿。任何流程变更须更新本文档并记录版本。*
