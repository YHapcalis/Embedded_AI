# 实现计划（Implementation Plan）

> 文件：`03_实现计划.md`
> 状态：**合并自** `01_目标定义.md` + `02_任务拆解.md`（保留原文档为历史记录）
> 锚点：`STANDARD_PROCESS.md`（标准流程 v0.2，12 步）—— 本计划的每个 Phase
>       明确"支撑流程的哪几步"，保证"实现即流程可用"。
> 原则：先定目标，后拆任务；每个任务可独立验收，验收项映射 S1-S10。

---

## Part A：目标定义

### A1 愿景（Vision）

打造一份"**可运行的嵌入式 AI 开发工作流包**"：
无论什么开发环境、哪个 AI 编程工具、哪块 MCU 芯片，
把工作流包交给任意 AI，它都能自行解读并执行 `STANDARD_PROCESS.md`
的 12 步流程，完成"需求 → 编码 → 编译烧录 → 验证 → 诊断修复 → 交付"闭环。

一句话：**让 AI 成为可迁移的嵌入式开发工程师，工作流是可携带的"岗位说明书+工具箱"。**

### A2 目标（Goal）

在 `E:\嵌入式AI工作流\` 交付可运行的工作流包，包含：

1. **指示文件**：`STANDARD_PROCESS.md`（12 步流程，AI 执行依据）✅ 已有 v0.2
2. **自引导蓝图**：`EMBEDDED_AI_WORKFLOW.md`（全貌 + 搭建指引）
3. **通用抽象层引擎**：硬件操作/编译烧录/测试验证/诊断修复（不绑定 MCU）
4. **MCP 服务**：引擎能力暴露为标准工具，任意 AI 工具可接入
5. **技能包知识库**：SKILL.md 路由 + 诊断技能 + 芯片参考 + SVD
6. **新工程模板**：harness init 多板卡骨架

**可验证表述**：新 AI 仅凭工作流包（无对话上下文），能按 STANDARD_PROCESS
12 步独立完成"从需求到交付"（以 F407 为参考验证平台）。

### A3 范围（Scope）

**包含**：
- 通用硬件抽象层（调试会话/内存寄存器读写/芯片探测/诊断引擎）
- 编译烧录封装（build/flash + verify）
- 测试断言引擎（YAML 场景，8 种断言）
- MCP 服务（connect/halt/read/diagnose/build/flash/test）
- 技能包（SKILL.md + 诊断技能 + chip-reference + SVD）
- 标准流程文档（STANDARD_PROCESS.md）✅
- 工程模板（多板卡骨架）
- 环境探测与配置管理（跨平台）
- OpenOCD 多分支管理（ST/Espressif/主线）✅ 部分（T0.3）
- F407 参考验证

**排除**：
- 具体 MCU 专用逻辑（用户按抽象层接口自行补充）
- GUI/IDE 插件（仅 CLI + MCP + 技能包）
- 云端/远程调试（本地为主）
- 商业级 CI/CD（脚本化即可）
- 具体外设驱动（摄像头/传感器）

**边界**：
- 抽象层接口稳定后冻结；新增 MCU 只加适配文件
- 诊断逻辑与芯片知识分离（换 MCU 加 chip-reference + SVD）
- git 版本化，随项目演进

### A4 成功标准（S1-S10）

| # | 验收项 | 判定方式 | 验收平台 |
|---|--------|---------|---------|
| S1 | 环境自检通过 | `harness env check` 全 OK（含目标芯片确认） | 本机 |
| S2 | 抽象层可连接硬件 | `connect()` 芯片探测 F407（CPUID=0x410FC241, DEV_ID=0x413） | F407 真机 |
| S3 | 健康检查 | halt 读寄存器正常，异常号=0，CFSR=0 | F407 真机 |
| S4 | 完整烧录 | 4 段 program verify 全 Verified OK | F407 真机 |
| S5 | 诊断能力 | HardFault 诊断输出完整报告（类型+位域+栈回溯+源码） | F407 真机 |
| S6 | 挂死诊断 | 5 次 PC 采样区分死循环/跑飞/正常运行 | F407 真机 |
| S7 | MCP 可接入 | AI 工具注册 MCP 成功，能调用 connect() | 本机 |
| S8 | 技能包可路由 | 新 AI 读 SKILL.md 按问题自动路由 | 本机 |
| S9 | 抽象层通用性 | 新增 MCU 仅加适配文件，不改引擎 | 代码评审 |
| S10 | 自引导完整性 | 新 AI 仅凭工作流包独立执行 12 步流程 | 模拟验证 |

### A5 约束

| 类型 | 约束 |
|------|------|
| 协议 | AixProbe 技能包 CC BY-NC-SA 4.0（非商用，可借鉴不可冒充） |
| 环境 | Windows/Linux/macOS；Python 3.10+ |
| 工具链 | OpenOCD 多分支 + 对应架构编译器；调试器 ST-Link/J-Link/CMSIS-DAP |
| 硬件 | F407ZGT6 参考平台；抽象层不得绑定型号 |
| 诚实 | 工具不可用如实报告；不假装成功 |
| 安全 | 硬件操作上限（默认 15 次/会话）；文件白名单；HardFault 检测 |

### A6 干系人

| 角色 | 期望 |
|------|------|
| 使用者（未来自己） | 丢给新 AI 即用，不重复搭建 |
| 执行者（未来新 AI） | 读蓝图 + STANDARD_PROCESS 能独立执行 |
| 验收者（现在自己） | 每阶段 F407 真机验收 |
| 受益者（面试官视角） | 可讲"嵌入式 AI 工作流标准化"故事 |

---

## Part B：任务拆解

### B0 问题清单（拆解输入）

| # | 问题 | 结论 | 对应任务 |
|---|------|------|---------|
| P1 | SWD 能否覆盖主流 MCU？ | 只覆盖 ARM，错过 ESP32/RISC-V | T1.2 |
| P2 | 是否预封装 JTAG？ | 必须，双协议接口不变 | T1.2 |
| P3 | CubeIDE 内置 OpenOCD 换芯片能用？ | 不能，需多分支共存 | T0.3 ✅ 已实现 |
| P4 | VSCode 定制 STM32 换芯片怎么办？ | 板卡 Profile + 自动生成配置 | T2.2, T2.3 |

### B1 流程 ↔ 实现映射（本计划核心）

```
STANDARD_PROCESS 12 步          支撑实现
─────────────────────          ─────────────────
① 确认芯片+环境检查      ←→   T0.2 env_probe + T0.3 Registry（✅）
② 技能包+MCP+编码规范    ←→   T3.1 MCP + T4.x 技能包
③ 项目上下文+原理图      ←→   T5.2 上下文文档模板 + 原理图解析工具（新增）
④ 拉起 AI 团队           ←→   T5.4 团队编排脚本（新增）
⑤ 理解需求               ←→   （流程文档指导，无代码依赖）
⑥ 风险与约束识别         ←→   （流程文档指导）
⑦ 目标定义+验收标准       ←→   T5.2 模板
⑧ 任务拆解               ←→   （流程文档指导）
⑨ 团队干活               ←→   T5.4 团队编排 + 文件白名单
⑩ 自主验收               ←→   T1.4 flasher + T1.5 tester + T1.3 diagnostics
⑪ 交付文档               ←→   T5.3 交付文档模板（新增）
⑫ 更新交接文档           ←→   T5.2 上下文文档闭环
```

> **本计划相对原拆解的补充**（因 STANDARD_PROCESS 而生）：
> - T5.2 升级：项目上下文文档模板（支撑 ③⑫ 闭环）
> - T5.4 新增：AI 团队编排（支撑 ④⑨）
> - T5.5 新增：交付文档模板（支撑 ⑪）
> - 原 02 拆解中的 T5.2（8 步流程）已由 STANDARD_PROCESS.md 取代

### B2 任务清单（Phase 0-6）

---

#### Phase 0：项目骨架与环境基线（✅ 已完成）

| 任务 | 内容 | 状态 |
|------|------|------|
| T0.1 | 项目初始化 + git | ✅ 已提交 |
| T0.2 | env_probe 环境探测（S1） | ✅ 已提交 |
| T0.3 | OpenOCD Registry 多分支（P3） | ✅ 已提交 |

**Phase 0 验收**：S1 部分满足（env check 6/6 OK，待 CLI 封装正式验证）

---

#### Phase 1：抽象层核心引擎（支撑 ⑩ 自主验收）

##### T1.1 调试会话抽象 SWDSession（S2, S3）
- **接口**：`connect / halt / resume / register_read / memory_read / memory_write / backtrace / chip_identify / read_fault`
- **前置**：所有操作先 connect()，未连接返回 "no active session"
- **产出**：`foundation/core/swd_session.py`（提炼 harness_ai SWDDebugSession，架构无关化）
- **验收**：S2 + S3
- **状态**：✅ 已完成（session.py）

##### T1.2 双协议支持设计（P1, P2, S9）
- **设计**：接口不变，transport（swd/jtag）由 Profile 决定底层 openocd.cfg
- **产出**：协议无关会话接口 + 新增架构说明文档
- **验收**：S9 代码评审
- **状态**：✅ 已完成（双协议由 Profile 决定，session/flasher 均实现）

##### T1.3 诊断引擎 diagnostics（S5, S6）
- **能力**：HardFault（CFSR 位域+栈回溯+ELF 映射）/ 挂死（PC 采样）/ 内存（SP+canary）
- **产出**：`foundation/core/diagnostics.py`（架构无关化）
- **验收**：S5 + S6
- **状态**：✅ 已完成（diagnostics.py）

##### T1.4 烧录封装 flasher（S4）
- **设计**：按 Profile 路由 OpenOCD 分支，多段 program verify
- **产出**：`foundation/core/flasher.py`
- **验收**：S4 F407 4 段 Verified OK
- **状态**：✅ 已完成（flasher.py，命令构造与已验证规范一致）

##### T1.5 测试断言引擎 tester
- **产出**：`foundation/core/tester.py` + scenarios 模板（复用 harness_ai expectations）
- **验收**：加载 YAML 场景并执行断言
- **状态**：✅ 已完成（tester.py，断言单测通过）

---

#### Phase 2：harness CLI 统一入口

##### T2.1 CLI 框架（S1 落地）
- **子命令**：`env check / init / build / flash / test / diagnose / openocd list / chip-confirm`
- **产出**：`foundation/cli/main.py` + `__main__.py`
- **验收**：全部子命令可调用，`--help` 完整

##### T2.2 板卡 Profile 机制（P4, S9）
- **Profile 字段**：mcu / arch / toolchain / transport / openocd / openocd_cfg / flash_sections
- **产出**：`boards/stm32f407/profile.json` + 加载器
- **验收**：新增板卡 = 新增 profile.json + cfg，引擎零改动
- **状态**：✅ 已完成（profile.py + boards/）

##### T2.3 VSCode 配置自动生成（P4）
- **设计**：`harness vscode-gen <board>` 生成 launch.json/tasks.json
- **产出**：`foundation/core/vscode_gen.py`
- **验收**：按 Profile 生成可用配置
- **状态**：✅ 已完成（vscode_gen.py，F407/ESP32-C3 双架构验证）

---

#### Phase 3：MCP 服务封装（支撑 ②）

##### T3.1 mcp_server.py（S7）
- **工具**：connect/halt/resume/memory_read/memory_write/register_read/chip_identify/diagnose/build/flash/test
- **产出**：`harness/mcp_server.py`
- **验收**：S7 AI 工具注册成功并调用
- **状态**：✅ 已完成（mcpservice/server.py，8 工具）

---

#### Phase 4：技能包知识库（支撑 ②）

##### T4.1 SKILL.md 路由（S8）
- **产出**：`skills/SKILL.md`（关键词映射 + 流程路由表）
- **验收**：S8
- **状态**：✅ 已完成（skills/SKILL.md）

##### T4.2 核心诊断技能文档
- **产出**：diagnosis-hardfault/hang/memory.md（基于 AixProbe 改编，遵守 CC BY-NC-SA）
- **验收**：每份含"流程+寄存器表+报告模板"
- **状态**：✅ 已完成（skills/ 10 文件）

##### T4.3 芯片参考 + SVD
- **产出**：`skills/chip-reference.md` + `skills/svd/arm/`
- **验收**：F407 SVD 就位，register-decode 可用
- **状态**：✅ 已完成（svd/arm/STM32F407xx.svd）

---

#### Phase 5：模板与标准流程（支撑 ③④⑪⑫）

##### T5.1 新工程模板
- **产出**：`templates/boards/stm32f407/` + `stm32f103/`
- **验收**：`harness init demo` 生成可编译骨架

##### T5.2 项目上下文文档模板 ★升级
- **产出**：`foundation/templates/project-context.md`（代码结构/构建系统/引脚表/模块清单/踩坑记录/未完成项）
- **验收**：新 AI 读后能理解项目全貌（支撑 ③⑫ 闭环）
- **状态**：✅ 已完成（提交 b721b47，附赠 coding-standard.md 编码规范模板）

##### T5.3 交付文档模板 ★新增
- **产出**：`foundation/templates/delivery-report.md`（固件/源码变更/设计说明/测试报告/使用手册/Changelog）
- **验收**：可套用生成完整交付包（支撑 ⑪）
- **状态**：✅ 已完成（提交 51acaf6）

##### T5.4 AI 团队编排脚本 ★新增
- **产出**：`foundation/core/team_orchestrator.py`（主理人 + 专业成员角色定义、任务分配、审查、白名单）
- **验收**：能按团队清单派发任务并收集产出（支撑 ④⑨）
- **状态**：✅ 已完成（提交 1649476）

##### T5.5 自引导蓝图修订
- **产出**：修订 `EMBEDDED_AI_WORKFLOW.md` 对齐本计划（v2.0）
- **验收**：蓝图与实现无矛盾，S10 前提满足
- **状态**：✅ 已完成（提交 51acaf6）

##### T5.5 自引导蓝图修订
- **产出**：修订 `EMBEDDED_AI_WORKFLOW.md` 对齐本计划
- **验收**：蓝图与实现无矛盾，S10 前提满足

---

#### Phase 6：全量验收

| 任务 | 操作 | 验收 |
|------|------|------|
| T6.1 | F407 真机全链路（环境→连接→健康→烧录→HardFault→挂死） | S1-S6 ✅ |
| T6.2 | MCP 接入 + 新增 MCU 评审 + 技能包路由 | S7-S9 ✅ |
| T6.3 | 模拟"新 AI 无上下文"独立执行 12 步流程 | S10 ✅ |

### B3 依赖关系

```
Phase 0 ✅ → T1.1 → T1.2 → T1.3 → T1.4 → T1.5
                   ↓
           T2.1 → T2.2 → T2.3
                   ↓
           T3.1
                   ↓
           T4.1 → T4.2 → T4.3
                   ↓
           T5.1 → T5.2 → T5.3 → T5.4 → T5.5
                   ↓
           T6.1 → T6.2 → T6.3
```

**可并行**：T4.x 与 T1.x 无代码依赖。

### B4 执行状态跟踪

| 任务 | 状态 | 提交 |
|------|------|------|
| T0.1 | ✅ | c888120 |
| T0.2 | ✅ | 17e0c8b |
| T0.3 | ✅ | 9f7bd53 |
| T1.1-T5.5 | ✅ 已完成 | 4105151..7234de1 |
| T6.1-T6.3 | ✅ 全部通过 | 7c1e2c6..1eda0f1 |

---

*本文件为项目实现主文档。原 `01_目标定义.md` / `02_任务拆解.md` 保留为历史记录，
后续变更直接修订本文件。*
